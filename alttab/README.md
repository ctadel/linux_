# AltTab

`config.plist` is the portable configuration exported from AltTab. AltTab
stores the literal keyboard shortcut in its shortcut registration rather than
as a portable preference key, so configure the next/previous window shortcuts
in AltTab's Preferences UI after installing on another machine.

Telemetry, license data, update state, window positions, and App Center IDs are
intentionally excluded.
